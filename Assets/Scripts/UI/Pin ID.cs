using Mapbox.Utils;
using UnityEngine;

public class PinID : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public string pinID;
    public Vector2d latLong;
}
