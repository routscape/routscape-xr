using Mapbox.Utils;
using UnityEngine;

public class UIGeodata : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public string itemID;
    public Vector2d latLong;
}
